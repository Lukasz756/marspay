spring:
  config:
    import: "optional:file:./.env[.properties]"

  datasource:
    url: "jdbc:postgresql://localhost:${MARSPAY_EARTH_DB_PORT}/${MARSPAY_EARTH_DB_NAME}"
    username: "${MARSPAY_EARTH_DB_USERNAME}"
    password: "${MARSPAY_EARTH_DB_PASSWORD}"

server:
  port: "${MARSPAY_EARTH_HTTP_PORT}"